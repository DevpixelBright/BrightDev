public class interrogateZuora
{
    @InvocableMethod
    public static void SubscriptionUpdateLateFee(List<Id> SubscriptionIds)
    {
        Boolean isNotFirst = false;
        Date todayDate = (datetime.valueOf(DateTime.now().format('yyyy-MM-dd HH:mm:ss', 'PST'))).date();
    } 
}